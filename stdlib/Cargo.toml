[package]
name = "zenith-stdlib"
version = "0.1.0"
edition = "2021"
description = "Zenith programming language standard library"
license = "MIT"
authors = ["Zenith Team"]

[dependencies]

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }
