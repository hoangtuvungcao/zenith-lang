[package]
name = "zenith-ui-linux"
version = "1.0.0"
edition = "2021"

[dependencies]
egui = "0.26"
eframe = { version = "0.26", default-features = true }
zenith-runtime = { path = "../../runtime" }

[features]
default = ["x11", "wayland"]
x11 = ["eframe/x11"]
wayland = ["eframe/wayland"]
